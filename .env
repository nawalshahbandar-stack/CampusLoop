DB_HOST=localhost
DB_PORT=5432
DB_NAME=campusloop
DB_USER=postgres
DB_PASSWORD=nawal321

# ==========================================
# EMAIL NOTIFICATIONS (Gmail)
# ==========================================
# EMAIL_USER: the Gmail address CampusLoop sends notices from.
# EMAIL_PASS: a Gmail App Password (NOT your normal Gmail password).
#
# How to get an App Password:
# 1. Go to https://myaccount.google.com/security
# 2. Turn on 2-Step Verification (required for App Passwords).
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Create an app password for "Mail" and paste the 16-character
#    code below (no spaces).
#
# If these are left blank, CampusLoop still works normally —
# it just skips sending notice emails and logs a message instead.

EMAIL_USER=youraddress@gmail.com
EMAIL_PASS=your16charapppassword
